// //
// // Select specific containers
// const introSection = document.getElementById('intro');
// const aboutSection = document.getElement('about');

// // Set initial offset values for each container
// let offset1 = 0;
// let offset2 = 0;
// // let offset3 = 0;

// // Your smoothScroll function
// function smoothScroll() {
// 	// Calculate offsets for each container (customize as needed)
// 	offset1 += (window.scrollY - offset1) * 0.04;
// 	offset2 += (window.scrollY - offset2) * 0.04;
// 	// offset3 += (window.scrollY - offset3) * 0.04;

// 	// Apply transformations to each container
// 	introSection.style.transform = `translateY(-${offset1}px) translateZ(0)`;
// 	aboutDiv2.style.transform = `translateY(-${offset2}px) translateZ(0)`;
// 	// container3.style.transform = `translateY(-${offset3}px) translateZ(0)`;

// 	// Request the next animation frame
// 	requestAnimationFrame(smoothScroll);
// }

// // Call smoothScroll initially
// smoothScroll();
